.FormGroup {
  display flex
  align-items flex-start
  width 100%
  position relative
  margin-bottom 60px
  
  &:last-child {
    margin-bottom 0
  }

  &--column {
    display flex
    flex-direction column
  }
  &--row {
    display flex
    justify-content space-between
  }
}